{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Json-server mock API for patient management system",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}

